int fun(int x, int y) {
  double z;
  z = x * y / 1.523;
  return z;
}
double opa(string z, int x) {
  double y;
  int aopa;
  if (z == "abc") {
    y = x * 39.42;
  } else {
    y = x * 53.234;
  }
  return y;
}
int opa(int y) {
  return y * 2;
}
{
  int x, y, x, f, g;
  double affers, f;
  string z;
  z = "abc";
  f = x * y / fun(g, x);
  if (f > 2) {
    f = f + 1;
  } else {
    while (y >= 0.1) {
      f = opa(z, f * 3);
      y = y - 1;
    }
  }
  affers = (f + g / y) * 34 - x * f;
  print(affers);
  read(x);
  g = fun(x, y) * affers;
}
